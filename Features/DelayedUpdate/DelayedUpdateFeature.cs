using Game.Systems;

namespace Game.Features
{
    public class DelayedUpdateFeature : Feature
    {
        protected override void BuildGroup()
        {
            // System(new RemoveDelayedUpdateActiveTagSystem());
            
            // System(new UpdateMaxFramesPerUpdateForEnemySystem());
            // System(new DecreaseDelayedUpdateFramesSystem());
            
            // System(new AddDelayedUpdateActiveSystem());
        }
    }
}