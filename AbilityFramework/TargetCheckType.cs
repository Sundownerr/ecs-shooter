namespace Game.AbilityComponents
{
    public enum TargetCheckType
    {
        None,           // targets count is 0
        AtLeastOne,     // targets count is >= 1
        AtLeastAmount   // targets count is >= amount
    }
}
