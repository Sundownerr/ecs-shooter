using Scellecs.Morpeh;

namespace Ability.Targeting
{
    public struct FromScript_AbilityTarget : IAbilityTarget
    {
        public void AddTo(Entity entity)
        {
            // Targets are added from script so we don't need to do anything here
        }
    }
}
